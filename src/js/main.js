/* eslint-disable no-undef */
/* eslint-disable no-unused-vars */

// Promo Slider
var promoSwiper = new Swiper('.promo-swiper', {
  effect: 'fade',
  fadeEffect: {
    crossFade: true
  },
  pagination: {
    el: '.promo-pagination',
    clickable: true
  }
})
